# `ripple-simple.js`

A simplified interface to the Ripple network through a Javascript library and RESTful API.


## Installation

1. Clone repository 
2. `npm install`
3. `node app.js`

## Testing

`grunt test`
