// var Validator = require('node-validator')n,
//   ripple = require('ripple-lib');

// Validator.prototype.isRippleAddress = function() {
//   if (!this.str || !ripple.UInt160.is_valid(this.str)) {
//     this.error(this.msg || this.str + ' is not a valid Ripple address');
//   }

//   return this;
// };

// module.exports = Validator;