{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Options",
  "description": "An Options object for preparing a transaction",
  "type": "object",
  "properties": {
    "sequence": {
      "description": "The sequence number, relative to the initiating account, of this transaction.",
      "type": "integer",
      "minimum": 0
    },
    "fixed_fee": {
      "description": "Fixed Fee",
      "$ref": "FloatString"
    },
    "max_fee": {
      "description": "Max Fee",
      "$ref": "FloatString"
    },
    "max_ledger_version": {
      "description": "Highest ledger sequence number that a transaction can appear in.",
      "type": "integer",
      "minimum": 0
    }
  },
  "not": {
    "description": "Fixed and max fee are mutually exclusive",
    "type": "object",
    "required": ["fixed_fee", "max_fee"]
  }
}
