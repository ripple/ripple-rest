{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Instructions",
  "description": "Instructions for executing a transaction",
  "type": "object",
  "properties": {
    "sequence": {
      "description": "The sequence number, relative to the initiating account, of this transaction.",
      "type": "integer",
      "minimum": 0
    },
    "fixedFee": {
      "description": "Fixed Fee",
      "$ref": "FloatString"
    },
    "maxFee": {
      "description": "Max Fee",
      "$ref": "FloatString"
    },
    "maxLedgerVersion": {
      "description": "Highest ledger sequence number that a transaction can appear in.",
      "type": "integer",
      "minimum": 0
    }
  },
  "not": {
    "description": "Fixed and max fee are mutually exclusive",
    "type": "object",
    "required": ["fixedFee", "maxFee"]
  }
}
